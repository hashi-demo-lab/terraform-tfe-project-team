name: Module Release

on:
  pull_request:
    types: [closed]

jobs:
  Module-Version-Release:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Fetch PR Comments
      id: fetch-comments
      uses: peter-evans/find-comment@v3
      with:
        issue-number: ${{ github.event.pull_request.number }}
        token: ${{ secrets.GITHUB_TOKEN }}
        body-includes: "SemVerType=" # Adjust this to match a specific pattern in comments

    - name: Process Comments
      run: |
        echo ${{ steps.fc.outputs.comment-id }}
        echo ${{ steps.fc.outputs.comment-body }}
        echo ${{ steps.fc.outputs.comment-author }}
        echo ${{ steps.fc.outputs.comment-created-at }}
        # Add your logic here to process comments and extract necessary information

    - name: Run Pipeline
      run: |
        # Use extracted information from comments to run your pipeline
        echo "Running pipeline with extracted information"
